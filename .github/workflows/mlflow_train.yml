name: MLflow Model Training CI

on:
  push:
    branches: [ main ]
    paths:
      - 'MLProject/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  train-and-upload:
    runs-on: ubuntu-latest
    
    # Memaksa semua perintah 'run' berjalan di dalam folder MLProject
    defaults:
      run:
        working-directory: ./MLProject

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12.7'
        cache: 'pip'
        cache-dependency-path: './MLProject/requirements.txt'
        
    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Train Model & Log to DagsHub
      env:
        DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_USER_TOKEN }}
      run: |
        # Menambahkan flag --experiment-name
        mlflow run . --env-manager=local --experiment-name="Model_Training_Experiment"

    - name: Upload temporary artifacts
      uses: actions/upload-artifact@v4
      with:
        name: model-and-plots
        path: |
          MLProject/**/*.pkl
          MLProject/**/*.png
        retention-days: 14
        
    - name: Create GitHub Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: model-v${{ github.run_number }}
        name: Model Training Run #${{ github.run_number }}
        files: |
          MLProject/**/*.pkl
          MLProject/**/*.png
        generate_release_notes: true
